{
  "name": "raj-agrisetu-x",
  "version": "1.0.0",
  "private": true,
  "description": "Evaluator-grade prototype for Rajasthan statewide AgriStack rollout",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "check": "node --check app.js && node --check data.js"
  },
  "engines": { "node": ">=20" }
}
